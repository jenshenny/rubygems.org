<% @title = t('sign_up') %>

<div class="form_links sign_up_links t-body">
  <p><%= link_to t('.have_account'), sign_in_path %></p>
</div>

<%= form_for @user do |form| %>
  <%= render :partial => "/users/#{@user.current_step}", :object => form %>
<% end %>
